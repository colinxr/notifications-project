import { BaseModel } from './service-store';
import { ModelStatic } from './service-store/types';
export declare const models: Record<string, any>;
export declare function registerModel(Model: ModelStatic<BaseModel>, store: {
    clientAlias: string;
}): void;
