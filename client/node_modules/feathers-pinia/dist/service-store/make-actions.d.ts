import type { ServiceStoreDefaultActions, MakeServiceActionsOptions } from './types';
import { StateTree, _GettersTree } from 'pinia';
import { BaseModel } from './base-model';
export declare function makeActions<M extends BaseModel = BaseModel, S extends StateTree = {}, G extends _GettersTree<S> = {}, A = {}>(options: MakeServiceActionsOptions<M, S, G, A>): ServiceStoreDefaultActions<M> & A;
